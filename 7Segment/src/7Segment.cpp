MultiDigit::operator const char*() const(unsigned int[] intArr){
    unsigned int arrayLength = sizeOf(intArr)/sizeOf(unsigned int);
    char[arrayLength] bcdOutput;
    char buffer;

    for(unsigned int i = 0; i < arrayLength - 1; ++i ) {

    }
}

char MultiDigit::getDigitChar const(unsigned int myInt, unsigned int row, unsigned int col) {
    if(row == 0 && col ==  0) {
        return ' ';
    } else if(row == 1) {

    }

}
